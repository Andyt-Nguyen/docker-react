language: node_js
sudo: required
services:
  - docker

before_install:
  - docker build -t andythenuge/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true andythenuge/docker-react npm run test
